# Normalize all text files to LF in the repo, CRLF on checkout (Windows default)
* text=auto eol=lf

# Force these to always be LF
*.ts   text eol=lf
*.tsx  text eol=lf
*.js   text eol=lf
*.jsx  text eol=lf
*.json text eol=lf
*.css  text eol=lf
*.html text eol=lf
*.md   text eol=lf

# Binaries — never convert
*.png  binary
*.jpg  binary
*.ico  binary
